"use strict";
{
    var log = console.log;
    // array
    var scores1 = [1, 2, 3]; // prefered.
    var scores2 = [1, 2, 3];
    function printArr(a1) { }
    // tuple, not array: NG(Not readable) -> just object/class
    // but usefull case : with destructing... in framework?
    var student = ["name", 21];
    log(student[0], student[1]);
}
